Write comprehensive unit tests for the provided source code. First, infer the programming language from the code. Then, detect or infer the appropriate test framework based on any context clues in the code (e.g., imports, annotations, or conventions); if none are evident, default to a standard one for that language (e.g., Jest for JavaScript/TypeScript, pytest for Python, JUnit for Java). Ensure tests cover normal cases, edge cases, error handling, and aim for high code coverage. Output only the test code in a fenced block matching the source language, with no additional explanations.
